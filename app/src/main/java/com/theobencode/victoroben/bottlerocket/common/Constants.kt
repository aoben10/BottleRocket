package com.theobencode.victoroben.bottlerocket.common

import com.theobencode.victoroben.bottlerocket.BuildConfig

class Constants {
    companion object {
        val APP_DEBUG = BuildConfig.DEBUG
        const val BASE_URL = BuildConfig.BASE_URL
    }
}